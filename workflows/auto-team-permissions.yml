name: Auto Assign Team Permissions

on:
  repository:
    types: [created]
  workflow_dispatch:
    inputs:
      repo_name:
        description: "Repo adi (bos birakilirsa tum repolara uygulanir)"
        required: false

jobs:
  set-team-permissions:
    runs-on: ubuntu-latest
    steps:
      - name: Yetki ata
        env:
          GH_TOKEN: ${{ secrets.ORG_ADMIN_TOKEN }}
        run: |
          ORG="AundeTeknik-Org"

          if [ "${{ github.event_name }}" = "repository" ]; then
            REPOS="${{ github.event.repository.name }}"
          elif [ -n "${{ github.event.inputs.repo_name }}" ]; then
            REPOS="${{ github.event.inputs.repo_name }}"
          else
            REPOS=$(gh repo list $ORG --json name --jq '.[].name')
          fi

          for REPO in $REPOS; do
            echo "--- $REPO ---"

            curl -s -X PUT \
              -H "Authorization: token $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/orgs/$ORG/teams/developer/repos/$ORG/$REPO" \
              -d '{"permission":"push"}'
            echo "Developer -> write"

            curl -s -X PUT \
              -H "Authorization: token $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/orgs/$ORG/teams/viewer/repos/$ORG/$REPO" \
              -d '{"permission":"pull"}'
            echo "Viewer -> read"
          done
